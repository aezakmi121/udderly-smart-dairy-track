import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udderlysmartdairytrack.app',
  appName: 'Udderly Smart Dairy Track',
  webDir: 'dist',
};

export default config;
